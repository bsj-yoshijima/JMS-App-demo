version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Build starting"
        - cp index.html /
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
      - index.html
  cache:
    paths: []